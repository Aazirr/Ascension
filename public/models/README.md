# Central Node Model

Place your central-node GLB file here:

- `public/models/central-node.glb`

The central bubble loader in the scene expects exactly this path by default.

If you want a different filename or path, update `CENTRAL_MODEL_PATH` in:
- `app/components/canvas/CentralNode.tsx`
